---
const { headline } = Astro.props as Props;
export type Props = { headline?: string };
---

<section class="max-w-4xl w-full flex flex-col gap-16">
    {headline && <h3 class="text-white text-2xl font-bold bg-blue-950 p-2 w-fit">{headline}</h3>}
    <slot />
</section>
